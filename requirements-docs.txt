# Documentation site dependencies.
#
# Pinned to the versions the site was actually built and verified against locally, so a CI build
# cannot start failing because an upstream release changed rendering or tightened strict mode.
# Dependabot does not watch this file; bump it deliberately and rebuild locally with
# `mkdocs build --strict` before committing a change.
#
#   pip install -r requirements-docs.txt
#   mkdocs serve

mkdocs==1.5.3
mkdocs-material==9.5.18
mkdocs-material-extensions==1.3.1
pymdown-extensions==10.21.2
